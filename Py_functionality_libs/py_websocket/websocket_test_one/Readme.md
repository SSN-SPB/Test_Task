In order to test websocket functionality, we can use the `websocket-client` 
library in Python. Below is an example of how to create a simple websocket client that connects to a websocket server and sends a message.
Run websocket_server.py in one terminal to start the websocket server, 
and then run websocket_client.py in another terminal to test the websocket client.
or websocket_client_sync.py for a synchronous version of the websocket client.


```python