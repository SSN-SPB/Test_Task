pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        echo "building"
        sleep 10
      }
    }
    stage('Test') {
    steps {
      scirpt {
         echo 'Checking Python Linux version...'
         sh 'python3 --version || python --version'
    }
  }
}
}
}
