const newString = "All is fine";

const checkStringLength = (testedString) => {
  return testedString.length;
};

console.log(checkStringLength(newString));

// expect(checkStringLength(newString)).to.be.greaterThan(11);
