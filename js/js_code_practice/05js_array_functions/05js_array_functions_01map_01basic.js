const numbers = [1, 2, 3, 4, 5];

const doubled_numbers = numbers.map((numbers) => {
  return numbers * 2;
});

const tripled_numbers = numbers.map((numbers) => numbers * 3);
console.log(numbers);
console.log(doubled_numbers);
console.log(tripled_numbers);
