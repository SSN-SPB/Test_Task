name: Cypress Tests

on:
  push:
    branches: [taf_cypress]
  pull_request:
  workflow_run:
    workflows: ["Run Python Script"]
    types:
      - completed

jobs:
  cypress-run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: switch to test directory
        run: cd./TAF_test/JS/cypress/cypress_demo02_schulte_tables/

      - name: Install dependencies
        run: npm ci

      - name: Run Cypress tests
        run: npx cypress run